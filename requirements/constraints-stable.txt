# Diarrhizer stable constraints for Windows + Python 3.12
# Usage:
#   pip install -c requirements/constraints-stable.txt -r requirements/base.txt
#
# This revision fixes the conflict:
#   pyannote.audio 3.3.2 -> speechbrain>=1.0.0
# while an older constraint forced speechbrain<1.0.0.

# Core stack pinned by WhisperX 3.3.1
whisperx==3.3.1
faster-whisper==1.1.0
ctranslate2==4.4.0

# Diarization stack required by WhisperX 3.3.1
pyannote.audio==3.3.2
pyannote.core==5.0.0
pyannote.database==5.1.3
pyannote.metrics==3.2.1
pyannote.pipeline==3.0.1
speechbrain>=1.0.0,<1.1

# PyTorch line — cuDNN 8 alignment
# WhisperX 3.3.1 requires ctranslate2<4.5.0 (cuDNN 8).
# torch <2.4.0 ships cuDNN 8 DLLs (compatible).
# torch >=2.4.0+cu124 ships cuDNN 9 DLLs (INCOMPATIBLE — causes
# "Could not locate cudnn_ops_infer64_8.dll" at transcribe time).
# Pin exact versions to reduce backtracking on Windows.
torch==2.3.1
torchaudio==2.3.1

# Supporting libraries
# setuptools<81 required: WhisperX 3.3.1 uses pkg_resources at runtime,
# which is absent in setuptools>=81.
setuptools<81
numpy<2.0
huggingface-hub<1.0
tokenizers<0.16
transformers==4.39.3
onnxruntime<1.20.0
safetensors<0.5.0
pandas>=2.2,<3.0

# IMPORTANT:
# Do not pin `av` here for now. On Windows, PyPI file availability for recent
# PyAV releases can vary, and forcing a specific version may push pip into a
# source build. Resolve `speechbrain` first; if `av` still builds from source,
# handle that as a separate PyAV/Windows issue.
